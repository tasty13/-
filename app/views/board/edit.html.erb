<form action="/update" method="post">	<!--get방식: 주소창에 보이게, post: 주소창에 안보이고 내부적으로 날아감-->
  <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
  <input name="id" value="<%= Post.find(params[:id]).id %>" type="hidden">
  제목: <input type="text" name="title" value="<%= @post.title %>"><br>
  내용: <input type="text" name="content" value="<%= @post.content %>"><br>
  <input type="submit" value="Submit">
</form>